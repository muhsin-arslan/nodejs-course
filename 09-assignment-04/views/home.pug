extends layouts/main-layout.pug

block content
    h1 Homepage

    ul
        if users.length > 0
            each user in users
                li #{user.firstName}
        else
            p There is no user yet.